server:
  port: 8085

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/akkiitsme/Micro-config-service
          clone-on-start: true

eureka:
  instance:
    hostname: localhost
